"""
MaskProcessor -- interactive mask annotation and generation for DFL XSeg training.

Provides a web-based UI (via FastAPI + static files) for creating, editing,
and exporting segmentation masks using SAM, Grounded SAM2, and BiSeNet.
"""

__version__ = "1.0.0"

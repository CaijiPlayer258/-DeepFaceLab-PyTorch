# DeepFaceLab-Torch 依赖列表
# 注意：PyTorch 需要单独安装，请参考 README.md
# 
# 安装步骤：
# 1. 先安装 PyTorch (CUDA版本):
#    python -m pip install torch torchvision --index-url https://download.pytorch.org/whl/cu126
# 2. 再安装其他依赖:
#    python -m pip install -r requirements.txt
# 3. 安装 PyQt-SiliconUI:
#    cd PyQt-SiliconUI-main && python -m pip install -e .

# 核心依赖
numpy>=1.24.0
opencv-python>=4.8.0
opencv-python-headless>=4.9.0
scipy>=1.11.0
Pillow>=10.0.0
matplotlib>=3.7.0

# PyQt5 UI
PyQt5>=5.15.10
pyperclip>=1.8.0
python-dateutil>=2.9.0

# 深度学习相关
# 注意：使用 onnxruntime-gpu 以启用 GPU 加速
onnxruntime-gpu>=1.16.0
# onnxruntime>=1.16.0  # CPU版本，不要同时安装
onnx>=1.15.0
watchdog>=4.0.0

# 人脸相关（insightface需要Visual C++ Build Tools，可选）
facexlib>=0.3.0
# insightface>=0.7.0  # 需要编译，建议使用conda安装

# 数据处理
h5py>=3.9.0
scikit-learn>=1.3.0
tqdm>=4.66.0
numba>=0.57.0

# 工具库
pathlib2>=2.3.7
requests>=2.31.0
PyYAML>=6.0

# GroundingDINO 和 SAM2 相关
torchvision>=0.16.0
timm>=0.9.0
transformers>=4.35.0

# 图像处理
albumentations>=2.0.0
easydict>=1.13
filterpy>=1.4.5
Cython>=3.0.0
